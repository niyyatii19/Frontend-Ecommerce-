import { FormCustomValidators } from './form-custom-validators';

describe('FormCustomValidators', () => {
  it('should create an instance', () => {
    expect(new FormCustomValidators()).toBeTruthy();
  });
});
